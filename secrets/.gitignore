# Only public keys are allowed in this directory.
# Private keys must NEVER be committed to a public repo.

# Block everything by default
*

# Allow public key exports
!federico-public.asc
!.gitignore
